---
name: video-director-breakdown
description: 短视频编导拆解 + 可拍摄脚本生成。输入对标视频链接或口播稿，自动拉取详情/逐字稿/关键画面，输出编导级拆解报告（前三秒三钩子、分镜拆解、脚本结构、目标人群、转化手法、可复用公式），然后基于拆解出的结构，写出「结构相同、内容不同」的、可直接用于拍摄的完整脚本（含分镜表 + 口播文案 + 字幕/花字）。触发词：「编导拆解」「拆解这个视频」「前三秒」「钩子」「这个视频为什么火」「脚本结构」「对标拆解」「扒脚本」「写脚本」「生成口播脚本」「写带货脚本」「写分镜」「照着这个写一条」。
whenToUse: 当用户发来对标视频链接或口播稿，要求拆解其钩子/前三秒/分镜/结构/人群/转化手法，并在拆解后照着结构写一条新主题的、可直接拍摄的脚本时使用。核心场景：对标爆款 → 拆解 → 写同结构不同内容的可拍摄脚本。
metadata:
  version: 3.2.0
  category: video-direction
  author: lxy
---

# 短视频编导拆解 + 可拍摄脚本生成

这个 skill 做两件事，缺一不可：

1. **拆解**：把一条对标视频拆成「钩子 + 分镜 + 结构 + 人群 + 手法 + 可复用公式」。
2. **生成**：套用拆出来的结构和公式，写一条**结构相同、内容不同**的、可直接拿去拍摄的脚本（含分镜表 + 口播文案 + 字幕/花字）。

你不是在做"内容概述"，你是在**把一条视频还原成可复制的工程图纸，然后照着图纸造一条新的**。

## 核心心法

1. **⚠️ 钩子是重中之重的重中之重**。前 3~5 秒决定生死：观众在这几秒内决定"继续看"还是"划走"。**钩子不过关，后面写得再好都是零**。拆解的第一产出是"钩子是什么"，生成的第一产出是"钩子是什么"，而且钩子必须**先确认通过，再往下写正文**。
2. **钩子必须确认到能拍**。一个钩子好不好，最终标准只有一条——**能不能让目标人群在第 3 秒停下来想看下去**。设计钩子时必须回答：它制造了什么悬念/反差/好奇/痛点？它戳中了谁？如果连你自己都说不出"为什么这个钩子能留人"，这个钩子就不合格，必须重写。
3. **三钩子同屏**。合格钩子 = 视觉钩子（画面）+ 话术钩子（台词）+ 文字覆盖（字幕/标题）三者第 1 秒同时命中，缺一个就掉一档。
4. **分镜是拍摄的语言**。拆解要拆到镜头级，生成要写到镜头级（摄制组照表就能拍）。
5. **结构可复制，内容必须换**。抄的是骨架（结构+手法+节奏），不是台词和画面——生成的新脚本要和原片同结构但全新内容。

---

## 第一步：拉取素材（配合 SocialDataX MCP）

用户给链接时，先用 SocialDataX MCP 拉取三样素材：

1. **视频详情**（标题/作者/粉丝/点赞/时长/话题）
   - 抖音：`mcp__socialdatax-douyin__douyin_get_video_detail_by_url`
   - 小红书：`mcp__socialdatax-xhs__xhs_get_note_detail_by_note_url`
   - B站：`mcp__socialdatax-bilibili__bilibili_get_content_detail_by_url`
2. **口播逐字稿**（带时间戳）
   - 抖音：`mcp__socialdatax-douyin__douyin_submit_video_speech_9851ef6f56a2`（返回 transcript.segments，含 start_ms/end_ms/text）
   - 小红书：`mcp__socialdatax-xhs__xhs_submit_video_speech_text_by_note_url`
3. **关键画面**（封面 + 前 3 秒首帧，用于判断视觉钩子和景别）
   - 详情里有 `cover_image_url` / `video.play_url`，ffmpeg 抽帧：`ffmpeg -y -v error -i "<play_url>" -frames:v 1 frame_0s.jpg`
   - 视觉桥（modlens_read_image）读画面，重点读清字幕文字、画面主体、景别

> 没有口播转文字工具或只有文字稿时，基于已有材料拆解，画面相关判断标注"推断"。

---

## 第二步：拆解（必答七大问题）

### 1. 前三秒钩子（⚠️ 最重要的环节，拆解的第一产出）

拆解报告必须把钩子放在最前面、单独拎出来重点分析。至少回答六个子问题：

| 子问题 | 拆解方法 |
|---|---|
| 视觉钩子 | 前 3 秒画面里看到什么（人/物/动作/场景） |
| 话术钩子 | 第一句话是什么 |
| 文字覆盖 | 字幕/标题写了什么 |
| 钩子类型 | 从钩子类型库判定（见 `references/hook-library.md`） |
| **为什么放前三秒** | **制造了什么悬念/反差/好奇/痛点，怎么让滑视频的人停手——这是钩子的灵魂，必须说透** |
| 画面作用 | 立信任 / 视觉冲击 / 辅助话术 |

**钩子质量判定**：拆解完必须给一句结论——"这个钩子强不强？强在哪？如果弱，弱在哪、怎么改会更强？"不能只描述不评价。

### 2. 分镜拆解（拆到镜头级）

把整条视频按时间切成镜头/段落，每个镜头记录六要素：

| 时间 | 景别 | 画面内容 | 台词/口播 | 字幕/花字 | 镜头语言 |
|---|---|---|---|---|---|
| 0-3s | 近景 | 摊主真人出镜 | 「我要是男的…」 | 「我要是男的」 | 固定·居中·自然光 |

**镜头语言**（景别/运镜/构图/灯光）参照 `references/shot-language.md`。每个镜头至少标注「景别 + 运镜 + 构图/灯光」三要素，让拆解能还原"这个镜头是怎么拍的"。

景别术语：大远景/远景/全景/中景/中近景/近景/特写。口播带货主打中近景（真人出镜）+ 特写（产品/动作），钩子镜头常用近景或中近景突出"人"。

### 3. 脚本结构

识别视频套用了哪个结构模板（见 `references/script-structures.md`），并画出结构链：
> 钩子 → 冲突 → 价值塑造 → 社会认同 → 价格锚点 → 成交

### 4. 目标人群画像

年龄段 / 性别 / 身份特征 / 核心焦虑或欲望 / 消费能力，每条判断都要落到具体台词证据。

### 5. 转化手法（含心理机制）

识别手法并点明心理机制：异议前置、社会认同（social proof）、价格锚点（anchoring）、从众转化、身份代入、损失厌恶（loss aversion）、稀缺性、框架效应、权威背书。

### 6. 数据佐证（如有）

点赞/收藏/评论/转发比例、粉丝量级——判断是爆款/常规/测试，数据低要判断"结构问题还是选题问题"。

### 7. 可复用公式

把视频抽象成一句可复用的结构公式，例如：
> 「身份反转钩子 → 老顾客社会认同 → 价值塑造 → 价格锚点 → 新顾客成交」

---

## 第三步：生成可拍摄脚本（最终交付物）

当用户要"照着写一条"，基于拆解出的结构公式 + 用户的新主题/新产品，产出**可直接拍摄的脚本**。这是本 skill 的核心交付物。

### ⚠️ 强制流程：钩子必须先确认，再写正文

生成脚本时**绝不允许一步到位直接输出完整脚本**。必须分两步：

**第 1 步：先给钩子候选，停下来让用户确认。**
- 基于拆解出的钩子类型（如"身份反转"），套钩子公式，写出 **2~3 个钩子候选**（每个都含：视觉画面 + 第一句台词 + 字幕）。
- 逐个标注：钩子类型 + 为什么能留人 + 戳中什么人群。
- **停下来问用户**：选哪个钩子？或要调整哪个？
- 用户确认（或指定）后，才进入第 2 步。

**第 2 步：按确认的钩子写完整脚本**（含分镜表 + 口播文案 + 字幕）。

> 例外：如果用户已明确给出主题且说"直接写"，可以跳过确认，但**必须在脚本里把钩子单独列为第一重点**，并主动标注"钩子如不满意可先只换钩子，正文结构不动"。

### 脚本格式

```markdown
## 🎬 脚本：《<新标题>》

### ⭐ 钩子（0-3s，本脚本第一重点）
- 视觉：<拍什么画面>
- 话术：<第一句台词>
- 文字：<字幕/花字>
- 钩子类型：<类型>
- 为什么能留人：<悬念/反差/痛点，戳中谁>

### 定位
- 平台/时长：<抖音 · 90 秒 · 竖屏 9:16>
- 目标人群：<人群>
- 核心信息：<一句话卖点/主张>

### 分镜表（照表拍摄）

| 镜号 | 时间 | 景别 | 画面内容（拍什么） | 口播文案（说什么） | 字幕/花字 | 镜头语言 |
|---|---|---|---|---|---|---|
| 1 | 0-3s | 近景 | <画面> | <台词> | <字幕> | 固定·居中·自然光 |
| 2 | 3-15s | 中近景 | <画面> | <台词> | <字幕> | 推·特写·浅景深 |
| ... | ... | ... | ... | ... | ... | |

### 结尾 CTA
- 话术：<促进行动>
- 文字：<屏幕提示>

### 拍摄提示
- 场景/道具/服装/灯光要点
- BGM 类型建议
```

**生成铁律**：
1. **钩子第一，先确认再动笔**——钩子候选给 2~3 个让用户选，确认后才写正文；钩子不过关，正文不开始。
2. **结构照搬，内容全换**——保留原片的钩子类型、段落顺序、节奏、转化手法，但台词、画面、字幕、产品全部换成新主题的。
3. **分镜表必须能直接拍**——每个镜头写清"拍什么 + 说什么 + 字幕是什么 + 多久 + 景别运镜灯光（镜头语言）"，摄制组照表就能开工。
4. **钩子必须用公式重写**——不照抄原片台词，而是用同一个钩子公式（如"身份反转"）换成新内容。
5. **口播文案要口语化**——能念出来，不是书面语；每句短，符合说话节奏。

---

## 输出报告格式（拆解阶段）

```markdown
## 🎬 编导拆解：《<标题>》

### 一句话定位
### 前三秒三钩子（视觉/话术/文字/类型/为什么）
### 分镜拆解（表格）
### 脚本结构（结构链）
### 目标人群（含证据链）
### 转化手法（含心理机制）
### 数据佐证（如有）
### 可复用公式
> <公式>

（用户要求写脚本时，接「第三步」输出可拍摄脚本）
```

## 关键原则

1. **⚠️ 钩子永远是第一优先级**。拆解先拆钩子，生成先定钩子，钩子先确认再动笔。任何情况下都不能把钩子当作正文的一个附属小节一带而过。
2. **禁止内容概述**——回答"为什么这么设计"，不复述"讲了什么"。
3. **每个判断落到证据**（台词、时间戳、画面元素、景别）。
4. **素材缺失要声明**（画面缺失时标注"推断"）。
5. **拆解必给公式，生成必给分镜表**——两个交付物缺一不可。
6. **结构复制、内容创新**——生成的新脚本绝不照抄原片台词和画面。
7. **中文输出**，编导/操盘手口吻，专业不说教。

## 钩子自检清单（每次设计钩子都逐条过）

一个钩子能不能用，逐条打勾，**有一条不满足就重写**：

- [ ] 前 3 秒内就说出口 / 出现在画面（不是铺垫 10 秒才进入正题）
- [ ] 制造了明确的**悬念 / 反差 / 好奇 / 痛点 / 冲突**（至少占一个）
- [ ] 戳中了**具体的目标人群**（能说清"谁会被这一句留下"）
- [ ] 三钩子同屏：画面 + 台词 + 字幕第 1 秒都在配合这个钩子
- [ ] 能用一句话说清"为什么这个钩子能留人"（说不出=不合格）
- [ ] 不是照抄原片，而是用同一钩子公式换的新内容
- [ ] 台词口语化、能直接念出来

**钩子不过关，正文不开始。** 这是编导的第一铁律。

## 字幕规范（评估和生成文字覆盖时参照）

- 单屏最多 **2 行**，每行 **3-5 个字**
- 加粗无衬线字体 + 黑色描边
- 关键词换色高亮（价格、数字、动词）
- 字幕时间与口播逐字对齐
- 85% 用户静音看，字幕必须独立于声音也能看懂钩子

## 与其它技能的分工

- 「内容策略类」（video-content-strategist / social）：它们回答"做什么内容/渠道怎么铺"，本技能回答"单条怎么拆、怎么照着写可拍摄脚本"。
- 「AI 视频复刻类」（video-prompt-reverse-engineer）：它们拆镜头/灯光/运镜用于 AI 生成，本技能拆钩子/分镜/结构/心理用于编导和实拍脚本（分镜的镜头语言参照它们的术语体系）。
- 「剪辑类」（ai-jianji / jianying-editor）：本技能产出"怎么编、怎么拍"，剪辑类产出"怎么剪"。
- 详细参考：`references/script-structures.md`（脚本结构模板）、`references/hook-library.md`（钩子公式库）、`references/shot-language.md`（镜头语言：景别/运镜/构图/灯光）。

## 与 jianying 融合（编导 → 剪辑一条龙）

本技能可产出**机器可读的脚本 JSON**（镜像 Markdown 分镜表），供
`jianying-autoedit-agent` 直接转换成 `edit-blueprint.json` 建剪映草稿，
实现「编导拆解 → 脚本 → 剪映成片」自动闭环。

- **脚本 JSON 规范**：`references/script-json-schema.md`
- **转换层**：`jianying-autoedit-agent/scripts/director_to_blueprint.py`
- **使用**：写完 Markdown 分镜表后，同步产出一份脚本 JSON（字段与分镜表
  逐一对应、互相自洽），再交给转换器 + 素材映射，即可自动建草稿。

> 强制：即使输出 JSON，钩子仍必须先确认（`goal.hook_type/hook_text/hook_why`），
> 确认后才写 `shots`。JSON 与 Markdown 分镜表必须是同一脚本的两种形态，严禁不一致。
